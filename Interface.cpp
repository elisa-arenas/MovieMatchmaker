#include "Interface.h"

Interface::Interface(sf::RenderWindow window) {
    //this->window = window;
}


void Interface::initializeWindow(sf::RenderWindow& window) {

}

void Interface::resetWindow(sf::RenderWindow &window) {

}